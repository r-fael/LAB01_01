name: Hello Flow

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Diga oi
        run: echo "Hello World!"
      - name: Diga valeu
        run: echo "Valeu - bye!"
